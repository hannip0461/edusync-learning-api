# 설계 결정

## D1 — SQL Server를 주 실행 대상으로 사용

잠금 힌트, 중복 오류 코드와 Classic ASP 연동이 SQL Server에 종속적이므로 SQL Server 2022와 PDO_SQLSRV를 사용한다. MySQL과의 차이는 별도 문서에 기록하며, 검증된 동작으로 간주하지 않는다.

## D2 — 이벤트 원장과 현재 상태를 분리

`learning_events`는 수락한 모든 이벤트를 보존하고 `lecture_progress`는 현재 진행 상태를 제공한다. 늦게 도착한 이벤트를 추적할 수 있으면서도 최신 이어보기 상태를 덮어쓰지 않게 한다.

## D3 — 현재 상태보다 이벤트를 먼저 저장하고 롤백 후 중복 판정

고유 키 `(source, event_id)`를 가진 이벤트를 먼저 저장한다. 오류 2601/2627이 발생하면 트랜잭션을 롤백한 뒤 해시를 새로 조회해 멱등 재요청과 상충하는 `payload`를 구분한다. 이 방식으로 INSERT 전 중복 조회에서 생길 수 있는 경쟁 조건을 피한다.

## D4 — 인증 단계에서 `source` 결정

학습자 Bearer 인증과 플레이어 HMAC 미들웨어가 설정값으로 `source`를 결정하며 클라이언트는 이를 전송할 수 없다. HMAC 서명은 원본 요청 본문과 타임스탬프를 대상으로 한다. 정적 자격 증명은 로컬 개발에서만 사용한다.

## D5 — 이어보기, 최대 시청 위치와 완료 시각에 서로 다른 규칙 적용

이어보기 위치는 정렬 기준상 최신 이벤트를 따르므로 뒤로 이동할 수도 있다. 최대 시청 위치는 줄어들지 않는다. 완료는 상태를 전환하는 값이 아니라 최초 완료 시각으로 기록한다.

## D6 — Classic ASP는 읽기 전용으로 유지하고 루프백 경계를 소스에서 강제

Classic ASP 어댑터는 매개변수화된 ADO BIGINT 매개변수와 IIS Application 연결 문자열을 사용한다. 소스와 JSON 예제는 계약 테스트로 확인하며 새로운 쓰기 의존성을 추가하지 않는다.

사용자 인증은 두지 않는다. 경계는 호출자의 네트워크 주소다. 다만 그 통제를 설치 스크립트의 `-IPAddress 127.0.0.1` 바인딩에만 두지 않는다. 바인딩은 배치 산출물이고, 넓히는 순간 인증 없는 조회 엔드포인트가 되는데 배포되는 소스에는 그것을 막을 것이 없었다. 그래서 `progress.asp`가 `REMOTE_ADDR`을 직접 검사해 `127.0.0.0/8`과 `::1`이 아닌 호출자를 403으로 거부한다. 검사는 쿼리 문자열을 읽기 전, 연결 문자열을 쓰기 전에 실행하며 계약 테스트가 그 순서를 고정한다.

`X-Forwarded-For` 같은 전달 헤더는 쓰지 않는다. 클라이언트가 지정할 수 있기 때문이다. 그 대가로 이 어댑터는 리버스 프록시 뒤에 둘 수 없다. 프록시가 TCP 상대가 되어 전달된 모든 요청이 통과한다. 프록시가 필요한 배치라면 주소 검사만으로는 부족하고 실제 인증이 필요하다.

## D7 — 애플리케이션 조립을 명시적으로 유지

이 서비스에는 Slim 4, 수동 객체 조립, PDO_SQLSRV와 기능별 PHP 테스트로 충분하다. ORM, 큐나 DI 프레임워크를 추가해도 SQL Server 트랜잭션 경계가 개선되지 않으며 복잡성만 늘어난다.

## D8 — 현재 상태 키 범위를 잠금

학습자·강의 고유 인덱스에 `UPDLOCK,HOLDLOCK`을 사용해 현재 상태 행의 존재 여부와 관계없이 요청을 직렬화한다. 이벤트를 저장한 뒤 잠금을 적용하므로 이벤트 원장과 현재 상태가 함께 커밋되거나 함께 롤백된다.

## D9 — 실행 시점에 의존하지 않는 DB 테스트

병렬 HTTP 작업자는 테스트 전용 DB 배리어에서 대기한 뒤 동시에 요청을 시작한다. 교착 상태는 한 행을 가진 테스트 테이블 두 개를 역순으로 갱신해 재현하며, 운영용 플래그나 고정 대기 시간에 의존하지 않는다. 모든 테스트 데이터는 실행별 고유 접두사를 사용하고 `finally` 블록에서 제거한다.

## D10 — HMAC 경로는 발행자를 인증하고 학습자 소유권은 검사하지 않는다

두 쓰기 경로의 인가 강도가 다르다. Bearer 경로는 토큰을 학습자 한 명(`APP_BEARER_LEARNER_ID`)에 묶고 본문의 `learner_id`가 다르면 403으로 거부한다. HMAC 경로는 그 검사를 하지 않는다.

이유는 자격 증명의 성질이다. `PLAYER_HMAC_SECRET`은 발행자 하나를 지목하고 학습자 식별자를 담지 않는다. 플레이어는 여러 학습자를 대신해 콜백을 보내므로 한 명에 묶으면 경로의 목적이 사라진다. 소유권 검사를 넣으려면 `PLAYER_LEARNER_ID` 같은 단일 학습자 결속을 새로 만들어야 하고, 그것은 이 경로가 존재하는 이유와 모순된다.

기각한 대안은 두 개다.

- **HMAC 경로에도 단일 학습자 결속을 추가한다.** 발행자가 학습자 한 명만 담당한다는 가정이 필요하다. 서버 대 서버 발행자에게 맞지 않는다.
- **서명 대상에 학습자 주체를 포함하고 발행자별 허용 학습자 목록을 둔다.** 소유권을 실제로 검사할 수 있는 유일한 방향이다. 다만 발행자 등록과 목록 관리라는 새 저장 구조가 필요하고, 그 요구가 이 저장소의 범위에 없다.

남는 결과를 명시한다. `PLAYER_HMAC_SECRET`을 가진 쪽은 임의의 `learner_id`로 이벤트를 쓸 수 있다. 비밀값은 신뢰 경계 안에서만 유통해야 한다. 멱등성 이름공간은 `authenticated_source`로 분리되므로 발행자가 다른 발행자의 이벤트를 덮어쓰거나 충돌시킬 수는 없다.

인가가 전무한 것은 아니다. HMAC 경로도 학습자와 강의 존재(404), `ACTIVE` 수강(403), 수강 기간 안의 `occurred_at`(422)을 요구한다. 검사하지 않는 것은 소유권 하나다.

`tests/integration.php`가 이 차이의 양쪽을 고정한다. Bearer 경로가 남의 `learner_id`를 403으로 거부하고 이벤트를 쓰지 않는 것, HMAC 경로가 같은 요청을 200으로 수락하고 이벤트를 하나 쓰는 것, 그리고 HMAC 경로도 존재하지 않는 학습자와 수강 없는 강의는 거부하는 것을 각각 확인한다.

## D11 — nonce 저장소를 두지 않고 유니크 제약으로 재전송을 흡수한다

HMAC 미들웨어는 타임스탬프 허용 오차(`HMAC_TOLERANCE_SECONDS`, 기본 300초)만 검사한다. nonce 저장소가 없으므로 그 창 안에서 같은 서명 요청을 재전송할 수 있다. `openapi.yaml`이 이 창을 명시한다.

재전송이 상태를 바꾸지 못하는 이유는 인증 계층이 아니라 데이터 계층에 있다. 서명은 타임스탬프와 원본 본문을 함께 덮으므로, 유효한 재전송은 본문이 바이트 단위로 같다는 뜻이고 따라서 `event_id`와 `payload_hash`도 같다. `UQ_learning_events_source_event_id`가 두 번째 삽입을 2601/2627로 막고, D3의 판정이 해시를 비교해 `{"applied": false, "duplicate": true}`를 반환한다. 진행 상태 갱신은 이벤트 삽입 뒤에 오므로 실행되지 않는다.

nonce 저장소를 기각한 이유는 그것이 이 등식 위에 두 번째 방어를 얹기만 한다는 점이다. 저장소를 두면 만료 정리, 저장소 자체의 동시성, 다중 인스턴스 공유라는 문제가 새로 생기는데, 막아야 할 상태 변화는 이미 없다.

이 결정에는 조건이 붙는다. **쓰기가 멱등이 아닌 부수 효과를 얻는 순간 이 등식은 깨진다.** 알림 발송, 외부 호출, 카운터 증가 같은 것을 쓰기 경로에 추가하려면 그때 nonce 저장소가 필요하다. 유니크 제약은 원장 중복만 막고 그 밖의 부수 효과는 막지 못한다.

`tests/integration.php`가 등식을 고정한다. 같은 타임스탬프와 같은 서명으로 같은 요청을 두 번 보내 두 번째가 `duplicate`로 처리되고 이벤트 수와 진행 상태가 그대로임을 확인한다.
